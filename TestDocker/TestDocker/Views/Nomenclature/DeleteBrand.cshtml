@model TestDocker.ViewsModels.BrandViewModel
@{
    ViewBag.Title = "Удаление Брэнда";
}
<div>
    <dl class="dl-horizontal">
        <dt>Имя</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    <div>
        @if (Model.BrandCollections.Count > 0)
        {
            <h4>
                Удалить не возможно <br />
                есть  коллекции используещие данный брэнд
            </h4>
        }
        else
        {
            <form asp-action="DeleteBrand" asp-route-id="@Model.Id" method="post">
                <div class="form-group">
                    <input type="submit" class="btn btn-danger" value="Удалить" />
                </div>
            </form>
        }
    </div>
</div>

